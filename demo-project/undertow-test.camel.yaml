- restConfiguration:
    component: platform-http
    contextPath: /api
- rest:
    id: rest-test
    bindingMode: json
    get:
      - id: get-test
        path: /users
        to: direct:direct-test
- route:
    id: route-test
    nodePrefixId: route-123
    from:
      id: direct-id
      uri: direct
      parameters:
        name: direct-get
      steps:
        - setBody:
            id: setBody-123
            expression:
              constant:
                id: constant-123
                expression: hello from the other side
